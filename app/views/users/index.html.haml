%h1 Listing users

%table
  %tr
    %th Name
    %th Password digest
    %th Email
    %th Phone
    %th Source
    %th Google token
    %th
    %th
    %th

  - @users.each do |user|
    %tr
      %td= user.name
      %td= user.password_digest
      %td= user.email
      %td= user.phone
      %td= user.source_id
      %td= user.google_token
      %td= link_to 'Show', user
      %td= link_to 'Edit', edit_user_path(user)
      %td= link_to 'Destroy', user, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New User', new_user_path
